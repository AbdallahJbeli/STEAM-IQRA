# Database configuration
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_db_password
DB_NAME=steam_iqra_v1_0
DB_PORT=5432

# JWT configuration
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=1d


# Server configuration
SERVER_PORT=5000
CLIENT_URL=http://localhost:5173
# Set to true when running behind a reverse proxy/load balancer (Nginx, Caddy, etc.)
TRUST_PROXY=false